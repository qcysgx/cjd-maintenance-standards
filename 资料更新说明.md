# 资料更新说明

1. 将新的 HTML 保养资料放到本网站文件夹的上一级（与现有资料放在一起）。
2. 文件名必须使用：`起始年-结束年_品牌_车型.html`，例如 `2027-2030_Jeep_牧马人.html`。单一年可写成 `2027-2027_Jeep_牧马人.html`。
3. 品牌字段当前支持 `Jeep`、`道奇`、`克莱斯勒`、`RAM`。若要增加新品牌，在 `scripts/sync-maintenance-data.mjs` 的 `brands` 列表和 `app/page.tsx` 的 `brandOrder` 列表各添加一项。
4. 在网站项目文件夹运行 `pnpm sync-data`，然后重新构建或发布网站。

同步程序会自动识别年份区间、品牌和车型，复制文档并刷新查询数据，无需手动编辑网页。
